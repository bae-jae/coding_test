#include <iostream>
#include <string>
#include <vector>

using namespace std;

vector<int> psum;

void calc_psum(string s)
{
	psum.clear();
	psum.push_back(s[s.size() - 1]);


	for(int i = s.size() - 2; i >=0; --i)
	{
	}
}
int main(void)
{
	int tc;
	cin >> tc;

	int mod = 10e9 + 7;
	cout << "test " << " " << mod;

	for(int test = 0; test < tc; ++test)
	{
		string s;
		cin >> s;

		long long ans = 0;

		for(int i = 0; i < s.size(); ++i)
		{
			ans += 
		}
	}
}
